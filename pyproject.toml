[project]
name = "aiusage"
version = "0.2.0"
description = "A small terminal dashboard for verified AI usage limits"
requires-python = ">=3.10"

[project.scripts]
aiusage = "aiusage.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

