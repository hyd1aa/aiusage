# Agent notes

Keep real and demo provider paths strictly separate. Real mode must never infer
or fabricate percentages or reset times. Demo mode must never call adapters or
inspect authentication material.

